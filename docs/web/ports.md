### Запросы без браузера
Браузер обращается к DNS, чтобы получить IP-адрес ресурса по URL. 
Так вот веб-приложения запускаются на определенных компьютерах, их также называют серверами. 
У каждого сервера есть свой адрес — IP.

📝 IP-адрес (Internet Protocol) — уникальный числовой идентификатор устройства в компьютерной сети.

Браузер не знает, к какому серверу делать запрос. А DNS знает. Браузер обменивает URL на IP 
и после делает запросы на конкретный адрес машины. 
Также на сервере можно запустить несколько веб-приложений. 
Чтобы обеспечить возможность определенным приложениям получать запросы, используют порты. 
Порт — это число. По умолчанию используются порты 80 и 443, а браузер подставляет их 
автоматически.

Например, мы можем указать, что приложение А будет получать запросы, которые придут на 
адрес 127.0.0.1 с портом 8081 (например, http://127.0.0.1:8081), а приложение Б будет 
получать запросы по адресу 127.0.0.1 с портом 8082.