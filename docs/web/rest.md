## REST
REST - Representational State Transfer

Web приложения работатют по запросам и ответам

REST это набор правил как передавать данные в рамках протокола HTTP

REST говорит о том, какие типы запросов используются для каких операций. 
Он описывает правила построения URL и то, в каких случаях какие коды ответов 
возвращать в ответе. Приложения, которые построены с учетом правил REST, 
называются RESTful-приложениями. 

😮 Базовые операции над объектом называются CRUD (**Create, Read, Update, Delete** — Создание, Чтение, Обновление, Удаление).

CRUD операции:
- Create (создание)  -> POST
- Read (чтение) -> GET
- Update (обновление) -> PUT
- Delete (удаление) -> DELETE

Например:

GET http://127.0.0.1:8080/book?id=10 — получаем данные о книге.
POST http://127.0.0.1:8080/book — создаем новую книгу.  
PUT http://127.0.0.1:8080/book?id=10 — редактируем книгу.  
DELETE http://127.0.0.1:8080/book?id=10 — удаляем книгу.  
Во всех адресах мы видим конкретный IP, а именно 127.0.0.1.

📝 IP-адрес (Internet Protocol) — уникальный числовой идентификатор устройства в компьютерной сети.

Также вы можете заметить, что приложение ожидает запросы, которые будем отправлять по порту 8080. После адреса и порта расположено название сущности «/book». Это означает, что мы хотим произвести операцию именно над сущностью «книга». А методы нам подсказывают, что за операции мы хотим выполнить.

Так, например, если мы хотим получить информацию о книге, то используем GET-запрос. А чтобы приложение понимало, какую книгу мы запрашиваем, мы указываем ее идентификатор (?id=10) как параметр запроса. Запросы для удаления действуют по такому же принципу. А вот запросы для создания и изменения книги (POST и PUT) подразумевают, что вы в запросе укажете тело, в котором опишете все поля для создаваемой книги, например.

Стоит отметить, что эти правила являются неформальными договоренностями.

### RESTful API
**Application Programming Interface (API)**, или программный интерфейс приложения, — это 
набор эндпоинтов, который позволяет одним программам работать с другими. 
API предусматривает, что программы могут работать в том числе и на разных компьютерах. 
В этом случае требуется организовать интерфейс API так, чтобы ПО могло запрашивать функции 
друг друга через сеть.

Эндпоинт (endpoint) - это точка доступа к веб-приложению, URL-адрес, на который можно сделать HTTP-запрос и веб-приложение его обработает.

Также API должно учитывать, что программы могут быть написаны на различных языках программирования и работать в разных операционных системах.

💡 Пример:

Бухгалтерское приложение для выставления счетов. Счета хранятся на сервере: мобильное приложение обращается к нему через API и показывает, сколько счетов выставлено за запрашиваемый период, какая сумма сделок, какой остаток на счете.

**Для чего используют RESTful API**

RESTful API - это архитектурный стиль для разработки веб-сервисов, который использует протокол HTTP для передачи данных и взаимодействия между клиентом и сервером.

Чаще всего RESTful API применяют:

1. Для связи мобильных приложений с серверными.
2. Для построения микросервисных серверных приложений. Это архитектурный подход, при котором большие приложения разбиваются на много маленьких частей.
3. Для предоставления доступа к программам сторонних разработчиков. Например, Stripe API позволяет программистам встраивать обработку платежей в свои приложения.  

Каждый запрос RESTful API сообщает о результатах работы числовыми кодами — HTTP-статусами, 
которые чакстично указаны выше.

RESTful API позволяет обмениваться текстовой информацией в специальных форматах (XML, YAML, JSON), а так же загружать или выгружать файлы с сервера на клиентскую сторону.



