### Controller
При создании RESTful-приложения необходимо обеспечить доступ к данным путем обработки запросов.
Приложение должно получать GET, POST, PUT и DELETE-запросы и реагировать на них, т. е. 
отдавать в ответ либо данные в формате JSON, либо необходимый код ответа.

В директории controller находятся классы контроллеров. 
Именно они принимают запросы, вызывают нужные сервисы, чтобы запустить необходимую логику. 
Как следствие, они отдают результат работы программы. 
Ниже представлен пример контроллера для пользователя.
```
@RequestMapping("user")
@RestController
public class UserController {
private final UserService userService;

public UserController(UserService userService) {
this.userService = userService;
}

@PostMapping
public ResponseEntity<User> createUser(@RequestBody User user) {
User createdUser = userService.createUser(user);
return ResponseEntity.ok(createdUser);
}

@GetMapping("{userId}")
public ResponseEntity<User> getUser(@PathVariable Long userId) {
User user = userService.getUserById(userId);
if(user == null) {
return ResponseEntity.notFound().build();
}
return ResponseEntity.ok(user);
}

@PutMapping()
public ResponseEntity<User> updateUser(@RequestBody User user) {
User updatedUser = userService.updateUser(user.getId(), user);
if(updatedUser == null) {
return ResponseEntity.notFound().build();
}
return ResponseEntity.ok(updatedUser);
}

@DeleteMapping("{userId}")
public ResponseEntity<User> deleteUser(@PathVariable Long userId) {...}
}
```
Классы контроллеров имеют аннотацию @RestController. 
Благодаря ей контроллеры попадают в контекст спринга, 
а также Spring получает возможность конвертировать Java-объекты в JSON.

📝 **Аннотация @RestController включает в себя аннотацию @Controller и @ResponseBody. 
Именно ResponseBody добавляет функционал, который позволяет конвертировать Java-объекты в JSON.**

Также в контроллере подключается и используется сервис. Именно контроллеры получают запросы и в зависимости от URL и метода определяют, какой метод (или методы) сервиса запускать.

У класса есть аннотация RequestMapping. С помощью нее можем указать, по какому адресу (URL) данный контроллер будет получать запросы. В нашем примере все запросы, которые мы отправим на адрес http://127.0.0.1/user, попадут в UserController.

В классе реализованы CRUD-методы. Они имеют аннотации PostMapping, GetMapping, PutMapping. Например, все методы, которые имеют аннотацию GetMapping, будут принимать только GET-запросы, PostMapping — POST-запросы.

Можно передавать параметры в URL или в теле запроса, если вы используете POST- или PUT-запросы. Чтобы принять параметр из URL-запроса, используется аннотация @PathVariable, как, например, в методе getUser. Если необходимо получить информацию из тела запроса, используют аннотацию @RequestBody, как в методе createUser.